package com.alice.entity;

import java.util.concurrent.Callable;

public class CallableThreadEntity implements Callable {
    @Override
    public Object call() throws Exception {
        return null;
    }
}
